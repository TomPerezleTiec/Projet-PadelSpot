services:
  jupyter:
    image: quay.io/jupyter/pyspark-notebook:latest
    container_name: padelspot_spark
    ports:
      - "8888:8888"
      - "4040:4040"
    volumes:
      - .:/home/jovyan/work
    environment:
      - JUPYTER_TOKEN=padel
    mem_limit: 8g